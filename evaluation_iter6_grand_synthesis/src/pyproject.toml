[project]
name = "walk-resolution-eval"
version = "0.1.0"
requires-python = ">=3.10,<3.12"
dependencies = [
    "numpy",
    "scipy",
    "pandas",
    "matplotlib",
    "loguru",
    "psutil",
    "jsonschema",
]
