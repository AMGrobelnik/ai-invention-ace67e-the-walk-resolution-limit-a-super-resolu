[project]
name = "eval-walk-resolution-limit"
version = "0.1.0"
requires-python = ">=3.10,<3.12"
dependencies = [
    "numpy",
    "scipy",
    "matplotlib",
    "loguru",
    "jsonschema",
    "psutil",
]
